# Required: replace with your current public IP (with /32 CIDR mask)
my_ip = "203.0.113.25/32"

# Optional: override defaults (uncomment and edit if needed)
# region          = "us-east-1"
# ami_name_filter = "amzn2-ami-hvm-*-gp2"
# instance_type   = "t3.large"    # t3.large for more capacity, c5.large for compute
# allowed_cidr    = "0.0.0.0/0"
# ssh_port        = 22
# react_port      = 3000
